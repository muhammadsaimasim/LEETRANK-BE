PORT = 0000
JWT_SECRET = "string"
MONGO_URI = "cluster_url"

SMTP_USER = email
SMTP_PASS = app_pass

SMTP_HOST = host
SMTP_PORT = 000
SMTP_SECURE = bool_type

# Secret token used to authorize requests to /api/cron/update-stats
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET = "your_random_secret_here"